# Phase1 Project

This repository contains the static files for the Phase1 website (HTML pages and assets).

Files included: HTML pages and source code. Large binary images are excluded by default via .gitignore.

To push to your remote repository:

1. Add a remote: git remote add origin <your-repo-url>
2. Push: git push -u origin master
